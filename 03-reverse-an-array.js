// User function Template for javascript
/**
 * @param {number[]} arr
 * @returns {number[]}
 */

class Solution {
	// Function to reverse the array.
	reverseArray(arr) {
		// your code here
		/*const temp = []
        for (let i = arr.length - 1; i >= 0; i--) {
            temp.push(arr[i]);
        }
        
        return temp; */

		arr.reverse();
		return arr;
	}
}
